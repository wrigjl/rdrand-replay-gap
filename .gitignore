# Build artifacts
target/
*.o
a.out
*.exe

# Binary tarballs (regenerate via Dockerfiles; do not commit)
*.txz
*.tar.xz
*.tar.gz

# Release-asset staging dir, populated by
# scripts/build_release_tarballs.sh and uploaded to GitHub Releases.
release-assets/

# rr traces
.rr-trace*/

# Editor
.DS_Store
*~
*.swp
